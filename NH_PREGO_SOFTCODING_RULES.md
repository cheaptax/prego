# 농협지원센터 관리자 편집 및 소프트코딩 규칙

## 1. 규칙의 목적

이 규칙은 농협지원센터 웹 프로젝트에서 새로 생성하거나 수정하는 모든 화면과 기능에 적용한다.

최초 개발 이후에는 비개발자 관리자가 `/admin`에서 사용자에게 보이는 콘텐츠, 디자인, 노출 구성과 안전한 운영 옵션을 직접 변경할 수 있어야 한다. 단순 문구 변경이나 색상 변경을 위해 개발자 코드 수정과 재배포가 필요하면 원칙적으로 미완성 기능으로 본다.

관리자 편집 기능은 개발 도구가 아니라 일반 업무용 편집 화면처럼 설계한다. 관리자가 JSON, CSS, HTML, 데이터베이스 경로, 컴포넌트 이름을 알아야 사용할 수 있는 구조는 허용하지 않는다.

## 2. 소프트코딩의 정의

이 프로젝트에서 소프트코딩은 다음을 의미한다.

- 사용자에게 표시되는 콘텐츠와 허용된 디자인 속성을 코드가 아닌 검증된 CMS 데이터로 관리한다.
- 관리자가 `/admin`에서 해당 데이터를 이해하기 쉬운 입력 화면으로 편집할 수 있다.
- 초안 저장, 미리보기, 게시, 이전 버전 복원이 가능하다.
- CMS 데이터가 없거나 잘못되어도 코드에 포함된 검증된 기본값으로 정상 동작한다.
- 설정 변경을 위해 일반적으로 코드 수정이나 재배포가 필요하지 않다.
- 보안과 데이터 무결성에 영향을 주는 핵심 기능 로직은 코드에서 보호한다.

소프트코딩은 모든 코드와 로직을 관리자가 임의로 변경하게 만드는 것이 아니다. 편집 가능한 부분과 보호해야 할 부분을 분리하는 것이 핵심이다.

## 3. 적용 대상

다음 항목은 새 화면이나 기능을 만들 때 처음부터 관리자 편집 대상으로 설계한다.

### 3.1 콘텐츠

- 페이지 제목, 부제목, 본문, 강조문구
- 버튼, 링크, 메뉴, 탭, 배지 문구
- 카드 제목과 설명
- 수치, 통계, 단계명과 안내문
- FAQ 질문과 답변
- 입력 필드의 표시 라벨, placeholder, 도움말
- 검증 실패, 성공, 오류, 로딩, 빈 화면 메시지
- 동의문구, 운영 안내, 면책문구
- 이미지 대체 텍스트와 접근성 라벨
- SEO 제목, 설명, OG 이미지와 검색 노출 여부

### 3.2 디자인

- 승인된 폰트 패밀리
- PC, 태블릿, 모바일별 글자 크기
- 글자 굵기, 줄간격, 자간, 정렬
- 글자색, 배경색, 강조색, 링크색
- 승인된 범위의 그라데이션
- 여백, 패딩, 요소 간 간격
- 테두리, 둥근 모서리, 그림자
- 콘텐츠 최대 폭과 정렬 방식
- 버튼의 승인된 크기, 색상, 형태
- 이미지 비율, 초점 위치, 캡션

### 3.3 화면 구성

- 섹션 표시와 숨김
- 섹션과 반복 항목의 순서 변경
- 카드, FAQ, 단계, 배너 등 반복 항목의 추가와 복제
- 복구 가능한 삭제와 복원
- CTA 버튼 표시, 문구와 목적지
- 공통 헤더, 내비게이션, 푸터와 고객지원 영역
- 허용된 범위의 기능 활성화와 비활성화

## 4. 코드에서 보호할 항목

다음 항목은 일반 관리자 편집 대상으로 노출하지 않는다.

- Firebase와 외부 서비스의 API 키와 비밀값
- 인증, 세션과 관리자 권한 판단 로직
- Firestore collection과 field의 실제 이름
- 폼의 내부 `name`, 저장 키와 API endpoint
- 데이터베이스 보안 규칙
- 개인정보 접근 권한
- 결제, 금액 계산, 평가점수 계산 등 핵심 계산식
- 감사로그 생성과 보존 방식
- 스팸 방지용 honeypot과 보안 장치
- 파일 업로드의 보안 제한
- 법적으로 필수인 동의 항목의 필수 여부
- 임의 JavaScript, HTML, CSS 실행 기능

관리자가 조절할 필요가 있는 기능 옵션은 개발자가 타입, 허용값, 최소값과 최대값을 먼저 정의한 후 안전한 설정 항목으로만 제공한다.

## 5. 신규 기능 개발의 필수 절차

새 페이지, 섹션 또는 기능을 개발할 때 다음 절차를 모두 수행한다.

1. 사용자에게 노출되는 콘텐츠와 디자인 속성을 먼저 식별한다.
2. 보호할 기능 로직과 관리자 편집 가능 항목을 구분한다.
3. 안정적인 `pageKey`, `featureKey`, `sectionId`를 정한다.
4. TypeScript 타입과 런타임 검증 스키마를 만든다.
5. 현재 화면을 정상 표시하는 코드 기본값을 만든다.
6. 초안 데이터와 게시 데이터를 분리한다.
7. `/admin`에 비개발자용 편집 화면을 함께 만든다.
8. 라이브 미리보기와 PC, 태블릿, 모바일 전환을 제공한다.
9. 초안 저장, 게시, 이력과 롤백을 연결한다.
10. 권한, fallback, 접근성, 성능과 회귀 테스트를 추가한다.
11. 관리자 사용 방법을 문서화한다.
12. 중앙 CMS 기능 등록부에 화면 이름, route, 접근 권한, schema, 기본 콘텐츠, 보호 대상, 미리보기와 관리자 메뉴를 등록한다.
13. `npm run cms:audit`로 App Router 사용자 화면과 중앙 등록부의 누락 여부를 검사한다.

화면만 먼저 하드코딩하고 관리자 편집 기능을 나중 작업으로 미루지 않는다. 관리자 편집 기능까지 완료되어야 해당 기능 개발이 완료된 것으로 본다.

## 6. CMS 데이터 원칙

### 6.1 공개 데이터와 초안 분리

공개 사용자가 읽을 수 있는 문서에는 초안, 내부 메모, 수정자 정보나 감사정보를 함께 저장하지 않는다.

기본 구조는 다음 원칙을 따른다.

- `cmsPublishedPages/{pageKey}`: 공개 가능한 페이지 게시본
- `cmsDraftPages/{pageKey}`: 관리자 전용 페이지 초안
- `cmsPublishedGlobals/{documentKey}`: 공개 가능한 공통 게시본
- `cmsDraftGlobals/{documentKey}`: 관리자 전용 공통 초안
- `cmsPageRevisions/{pageKey}/revisions/{revisionId}`: 관리자 전용 버전 이력
- `cmsAuditLogs/{logId}`: 관리자 전용 작업 이력
- `cmsAssets/{assetId}`: 미디어 메타데이터

프로젝트 구조상 다른 명칭을 사용하더라도 공개본, 초안, 이력과 감사로그의 권한 분리는 유지한다.

### 6.2 문서 필수 정보

CMS 문서에는 필요한 범위에서 다음 정보를 포함한다.

- `schemaVersion`
- `pageKey` 또는 `documentKey`
- `route`
- `content`
- `theme` 또는 검증된 `styleOverrides`
- `version`
- `status`
- `createdAt`, `createdBy`
- `updatedAt`, `updatedBy`
- `publishedAt`, `publishedBy`

시간은 서버 timestamp를 사용한다. 변경 충돌을 방지하기 위해 버전 또는 최종 수정 시각을 검증한다.

### 6.3 안정적인 식별자

- `pageKey`, `featureKey`, `sectionId`는 표시 문구와 분리한다.
- 제목이나 URL 문구가 바뀌어도 식별자가 바뀌지 않아야 한다.
- 배열 항목도 안정적인 고유 ID를 가진다.
- 컴포넌트 순서나 표시 문구를 데이터베이스 키로 사용하지 않는다.

### 6.4 스키마와 마이그레이션

- 모든 CMS 데이터는 TypeScript 타입과 런타임 스키마로 검증한다.
- 스키마에는 `schemaVersion`을 포함한다.
- 스키마 변경 시 이전 데이터의 마이그레이션 또는 하위 호환 처리를 함께 만든다.
- 마이그레이션 스크립트는 반복 실행해도 안전해야 한다.
- 운영 데이터를 자동으로 덮어쓰지 않는다.
- CMS 데이터가 없거나 검증에 실패하면 코드 기본값을 사용한다.

## 7. 관리자 화면 사용성 규칙

`/admin`은 비개발자가 별도 교육 없이 기본 작업을 수행할 수 있어야 한다.

### 7.1 화면과 용어

- 데이터베이스와 코드 용어 대신 업무 용어를 사용한다.
- `hero`, `array`, `boolean`, `slug` 같은 개발 용어를 그대로 노출하지 않는다.
- 예: `heroTitle` 대신 `첫 화면 큰 제목`, `isVisible` 대신 `이 영역 표시`를 사용한다.
- 각 설정에는 짧은 설명과 필요한 경우 예시를 제공한다.
- 고급 설정은 접힌 영역에 두고 기본 편집 흐름을 방해하지 않는다.
- 입력 항목을 의미 있는 단위로 묶고 한 화면에 과도하게 표시하지 않는다.

### 7.2 편집 방식

- 페이지 목록에서 편집할 화면을 URL과 미리보기 이미지로 쉽게 찾을 수 있게 한다.
- 좌측에는 페이지와 영역 목록, 중앙에는 미리보기, 우측에는 선택한 영역의 설정을 제공하는 방식을 우선한다.
- 실제 화면에서 영역을 선택하여 해당 편집 항목으로 이동할 수 있게 한다.
- 색상은 색상 선택기와 디자인 토큰 선택을 제공한다.
- 글자 크기는 숫자만 노출하지 말고 `작게`, `기본`, `크게` 프리셋과 세부 설정을 함께 제공한다.
- 링크는 내부 페이지 선택과 외부 주소 입력을 구분한다.
- 이미지 업로드 시 권장 크기와 비율을 안내한다.
- 반복 항목은 드래그로 순서를 변경할 수 있게 한다.
- 삭제는 복구 가능하게 처리하고 삭제 전 대상을 명확히 표시한다.

### 7.3 저장과 게시

- 편집 중 변경사항은 초안으로 자동 저장한다.
- 초안 저장과 실제 게시를 명확하게 구분한다.
- 게시되지 않은 변경이 있으면 항상 눈에 띄게 표시한다.
- 게시 전 변경 전후를 비교할 수 있게 한다.
- 게시 시 영향을 받는 페이지를 알려준다.
- 실행 취소, 다시 실행과 이전 버전 복원을 제공한다.
- 다른 관리자가 같은 화면을 수정한 경우 충돌 경고를 표시한다.
- 저장 실패 시 원인과 사용자가 해야 할 조치를 쉬운 문장으로 안내한다.

### 7.4 미리보기

- PC, 태블릿, 모바일 미리보기를 제공한다.
- 미리보기는 실제 공개 페이지 렌더러와 동일한 컴포넌트를 사용한다.
- 초안 미리보기는 관리자만 접근할 수 있어야 한다.
- 편집값이 실제 화면에 최대한 즉시 반영되어야 한다.
- 게시 전 깨진 링크, 빈 필수 문구, 이미지 대체 텍스트 누락과 색상 대비 문제를 경고한다.

### 7.5 오류 방지

- JSON, raw HTML, raw CSS 입력창을 일반 관리자에게 제공하지 않는다.
- 잘못된 값은 저장 버튼을 누른 뒤가 아니라 입력 시점에 설명한다.
- 색상, 글자 크기, 여백과 숫자 설정에는 허용 범위를 둔다.
- 서비스가 깨질 수 있는 항목에는 안전한 기본값과 초기화 기능을 제공한다.
- 필수 섹션과 법적 필수문구는 숨김 또는 영구삭제할 수 없게 한다.

## 8. 디자인 소프트코딩 규칙

- 디자인 변경은 CSS 변수 또는 타입이 있는 디자인 토큰으로 변환한다.
- 자유형 CSS 입력을 허용하지 않는다.
- 승인된 폰트 목록을 사용한다.
- 임의 외부 폰트 URL을 입력하게 하지 않는다.
- 반응형 값은 PC, 태블릿, 모바일 단위로 관리한다.
- 페이지 전체 기본 디자인과 개별 섹션 덮어쓰기를 구분한다.
- 기본 디자인을 변경하면 영향을 받는 화면을 관리자에게 알린다.
- 개별 설정을 `기본 디자인 사용` 상태로 되돌릴 수 있게 한다.
- 기존 프로젝트의 디자인 시스템을 우선 재사용한다.

## 9. 폼과 기능 설정 규칙

폼 화면에서는 다음 항목을 편집할 수 있다.

- 표시 라벨
- placeholder
- 도움말
- 버튼 문구
- 성공, 실패와 검증 안내문
- 관리자가 선택 가능한 안전한 노출 옵션
- 개발자가 범위를 정한 최소값, 최대값과 선택지

다음 항목은 보호한다.

- 내부 field name
- Firestore 저장 키
- API endpoint
- 인증과 권한 확인
- honeypot과 rate limit
- 필수 개인정보 동의의 필수 여부
- 민감정보 저장 방식

선택지를 관리자가 추가할 수 있는 기능은 저장 가능한 값과 화면 표시 문구를 분리한다. 기존 데이터와 연결된 값은 경고 없이 변경하거나 삭제할 수 없게 한다.

## 10. 공통 요소 규칙

다음 요소는 페이지마다 복사해 관리하지 않고 공통 CMS 영역으로 관리한다.

- 로고와 서비스명
- 헤더와 내비게이션
- 로그인, 회원가입과 주요 CTA
- 푸터
- 고객지원 버튼
- 공통 연락처와 운영 주체
- 기본 SEO 설정
- 기본 색상, 폰트와 버튼 디자인

페이지에서 공통값을 다르게 표시해야 하는 경우에만 명시적인 페이지별 덮어쓰기를 허용한다.

## 11. 보안 규칙

- 이메일 문자열만으로 관리자 권한을 판단하지 않는다.
- Firebase custom claim 등 검증된 역할 정보를 사용한다.
- 관리자 화면 진입과 데이터 쓰기에서 각각 권한을 검증한다.
- 초안, 이력과 감사로그는 관리자만 읽고 쓸 수 있다.
- 공개 사용자는 게시본만 읽을 수 있다.
- rich text는 허용된 태그와 속성만 sanitize한다.
- `script`, event handler, `javascript:` URL을 차단한다.
- 이미지와 파일은 종류, 크기와 확장자를 검증한다.
- 비밀번호, 토큰, 개인정보를 로그에 남기지 않는다.
- 관리자 작업에는 수정자, 시각, 대상과 작업 종류를 기록한다.

## 12. 성능과 안정성 규칙

- 페이지의 CMS 데이터는 가능한 한 페이지 단위로 한 번에 조회한다.
- 화면 요소마다 Firestore read를 발생시키지 않는다.
- 현재 프레임워크의 서버 렌더링, cache와 revalidation 기능을 활용한다.
- 게시 시 변경된 페이지만 cache invalidate한다.
- CMS 로딩 때문에 기존 화면이 깜박이거나 크게 이동하지 않게 한다.
- CMS 장애 시 코드 기본값으로 계속 서비스한다.
- CMS 오류는 사용자에게 기술적인 메시지로 노출하지 않는다.
- 현재 폼 제출, 인증, 저장과 조회 기능의 동작을 유지한다.

## 13. 접근성 규칙

- 모든 이미지에 대체 텍스트 입력란을 제공한다.
- 버튼과 링크의 접근 가능한 이름이 비어 있지 않게 한다.
- 색상 변경 시 WCAG 대비 경고를 제공한다.
- 제목 계층이 잘못되면 게시 전에 경고한다.
- 키보드만으로 관리자 편집과 공개 화면의 주요 기능을 사용할 수 있게 한다.
- 오류 메시지를 색상만으로 구분하지 않는다.

## 14. 테스트 규칙

새 페이지 또는 기능에는 다음 테스트를 필요한 범위에서 포함한다.

- CMS 스키마 검증 테스트
- 코드 기본값 fallback 테스트
- 초안과 게시본 분리 테스트
- 일반 사용자의 초안 접근 차단 테스트
- 관리자 저장, 게시와 롤백 테스트
- 기존 기능 payload와 저장 키 회귀 테스트
- PC, 태블릿과 모바일 미리보기 테스트
- 로딩, 빈 데이터, 오류와 권한 없음 상태 테스트
- 접근성 기본 검사
- typecheck, lint와 production build

## 15. 완료 조건

새 페이지나 기능은 다음 질문에 모두 `예`라고 답할 수 있어야 완료다.

- 비개발자가 `/admin`에서 이 화면을 찾을 수 있는가?
- 사용자에게 보이는 주요 문구를 코드 수정 없이 바꿀 수 있는가?
- 글자, 색상, 간격, 이미지와 노출 구성을 안전하게 바꿀 수 있는가?
- 초안으로 저장하고 실제 화면과 유사하게 미리볼 수 있는가?
- 게시와 이전 버전 복원이 가능한가?
- 데이터가 없어도 코드 기본값으로 정상 표시되는가?
- 초안과 관리자 정보가 일반 사용자에게 노출되지 않는가?
- 핵심 기능 로직과 저장 키가 관리자 실수로 변경되지 않는가?
- 모바일과 접근성 검증을 통과했는가?
- 관리자 사용법이 문서화되어 있는가?
- 중앙 CMS 등록부와 자동 검사에 포함되어 있는가?

하나라도 `아니오`이면 해당 기능은 소프트코딩 원칙에 따라 완료되지 않은 것으로 본다.

## 16. 금지 패턴

다음 구현은 허용하지 않는다.

- 사용자 노출 문구를 페이지 컴포넌트에 직접 반복 하드코딩
- 콘텐츠 변경을 위해 개발자 배포가 필요한 구조
- CMS 문서가 없으면 페이지 전체가 오류 나는 구조
- 공개 문서 안에 초안과 내부정보를 함께 저장
- 관리자 화면에서 JSON이나 CSS를 직접 수정하게 하는 구조
- 이메일 주소만 비교하는 관리자 인증
- 문구와 URL을 데이터 식별자로 사용하는 구조
- 스키마 검증 없이 Firestore 데이터를 바로 렌더링
- 수정 이력 없이 게시본을 즉시 덮어쓰기
- 운영 데이터에 자동으로 seed 또는 migration 실행
- 새 기능을 만든 뒤 관리자 편집 기능을 별도 미래 작업으로 남기는 방식

## 17. 규칙의 적용과 예외

- 모든 신규 페이지, 기능, 이벤트 페이지와 공통 컴포넌트에 이 규칙을 적용한다.
- 기존 기능 수정 시 해당 범위의 하드코딩도 함께 소프트코딩으로 전환한다.
- 규칙을 적용하지 못하는 항목은 코드 주석이 아니라 개발 문서에 사유, 영향과 향후 처리방안을 기록한다.
- 보안, 법적 의무, 데이터 무결성 또는 성능상 이유가 있는 경우에만 예외를 허용한다.
- 예외가 있더라도 사용자가 보는 설명 문구와 안전한 운영 옵션은 가능한 범위에서 관리자 편집 대상으로 제공한다.
